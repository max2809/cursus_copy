services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: studybuddy
      POSTGRES_PASSWORD: studybuddy
      POSTGRES_DB: studybuddy
    ports:
      - "5432:5432"
    volumes:
      - studybuddy_pg_data:/var/lib/postgresql/data
volumes:
  studybuddy_pg_data:
