{
  "$schema": "https://railway.com/railway.schema.json",
  "deploy": {
    "startCommand": "alembic upgrade head && uvicorn studybuddy.main:app --host 0.0.0.0 --port $PORT --limit-max-requests 200 --timeout-graceful-shutdown 20"
  }
}
